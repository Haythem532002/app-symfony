{% extends 'base.html.twig' %}
{% block body %}
<div class="container">
    <h1>Connexion</h1>
    {# création d'un formulaire dans cette vue #}
    <form action="{{ path('login_user_check') }}" method="post">
        <div class="form-group">
            <input placeholder="Your mail..." required name="_username" type="text" class="form-control">
        </div>
        <div class="form-group">
            <input placeholder="Your password..." required name="_password" type="password" class="form-control">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-succeess">
                Connexion !
            </button>
        </div>
        <a href="{{ path('security_registration') }}"> Pas encore inscrit ?</a>
    </form>
</div >
{% endblock %}